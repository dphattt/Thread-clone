import { BrowserRouter as Router, Routes, Route } from "react-router";

function AppRoutes() {
  return (
    <Router>
      <Routes>
        <Route path="/" element={} ></Route>
      </Routes>
    </Router>
  );
}
